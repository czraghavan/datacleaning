"""
src.layers — 10-layer deterministic data transformation pipeline.

Each layer is a separate module with a single responsibility.
Layers must be executed in order and persist artifacts at every stage.
"""
